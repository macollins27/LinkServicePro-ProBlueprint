PROMPT: “Refactor two sections on industry pages: PainSolution + FeatureGrid (professional layout)”

Scope

Edit only the industry template sections:

client/src/sections/industries/IndustryPainSolution.tsx

client/src/sections/industries/IndustryFeatureGrid.tsx

Use existing data structures from INDUSTRY_DETAILS:

pains: { pain: string; fix: string }[]

features: { label: string; minPlan: 'Free'|'Starter'|'Essential'|'Premium'|'Platinum'; blurb?: string }[]

Do not change routing or other sections.

A) Replace IndustryPainSolution.tsx with this behavior

Layout

<section aria-label="Common pain points, solved" className="bg-slate-50">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14">
    <div className="text-center mb-6 sm:mb-8">
      <h2 className="text-xl sm:text-2xl font-semibold">Common pain points, solved</h2>
      <p className="text-slate-600 mt-1">See how LinkServicePro tackles your biggest challenges</p>
    </div>

    {/* choose grid size based on item count */}
    <div
      className={clsx(
        "grid gap-4 sm:gap-5",
        pains.length <= 4 ? "grid-cols-1 md:grid-cols-2" : "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
      )}
    >
      {pains.map(({ pain, fix }, i) => (
        <article
          key={i}
          className="rounded-2xl border border-slate-200 bg-white shadow-sm transition duration-150 ease-out hover:shadow-md hover:-translate-y-0.5"
        >
          <div className="p-4 sm:p-5">
            <h3 className="text-[15px] sm:text-base font-medium text-slate-900 leading-snug">
              {pain}
            </h3>
            <div className="mt-3 mb-2 border-t border-slate-200" />
            <p className="text-sm text-slate-600 inline-flex items-center gap-2">
              <CheckCircle2 className="w-4 h-4 text-emerald-600" aria-hidden="true" />
              {fix}
            </p>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>


Remove any red dot, arrow-underline, or empty placeholders.

If pains.length is 4, you will get 2×2. If 5–6, 2×3. No empty slots.

B) Replace IndustryFeatureGrid.tsx with this behavior

Helpers

const PLAN_STYLES: Record<string, string> = {
  Free:      "bg-slate-100 text-slate-700",
  Starter:   "bg-blue-100 text-blue-700",
  Essential: "bg-emerald-100 text-emerald-700",
  Premium:   "bg-violet-100 text-violet-700",
  Platinum:  "bg-amber-100 text-amber-800",
};


Layout

<section aria-label="Every feature you need">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14">
    <div className="text-center mb-6 sm:mb-8">
      <h2 className="text-xl sm:text-2xl font-semibold">Every feature you need</h2>
      <p className="text-slate-600 mt-1">Start free, upgrade as you grow</p>
    </div>

    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-5">
      {features.map(({ label, minPlan, blurb }, i) => (
        <article
          key={i}
          className="rounded-2xl border border-slate-200 bg-white shadow-sm transition duration-150 ease-out hover:shadow-md hover:-translate-y-0.5"
        >
          <div className="p-4 sm:p-5 h-full">
            {/* top row */}
            <div className="flex items-start justify-between gap-3">
              <div className="text-slate-400">
                <Plug className="w-6 h-6 sm:w-7 sm:h-7" aria-hidden="true" />
              </div>
              <span className={clsx("min-w-[74px] px-2 py-1 rounded-full text-xs font-medium", PLAN_STYLES[minPlan])}>
                {minPlan}
              </span>
            </div>

            {/* title + blurb */}
            <h3 className="mt-3 text-[15px] sm:text-base font-medium text-slate-900 leading-snug line-clamp-1">
              {label}
            </h3>
            {blurb ? (
              <p className="mt-1 text-sm text-slate-600 line-clamp-2">{blurb}</p>
            ) : null}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>


Badges are always in the top-right.

Title font is larger and single-line.

Optional blurb in text-sm, two lines max.

All icons are the same size for alignment.

Accessibility and polish

Keep keyboard focus rings (focus-visible:ring-2 focus-visible:ring-[#0B5FFF] focus-visible:ring-offset-2) on both card types if your project pattern prefers focusable button/a. If cards are static article, not needed.

Respect prefers-reduced-motion: append motion-reduce:transition-none motion-reduce:hover:transform-none motion-reduce:hover:shadow-sm to card classNames.

Ensure no CLS: no images in these cards, just icons and text.

Acceptance criteria

Pain section shows 2×2 if there are 4 items; 2×3 if 5–6. No empty placeholders.

Cards have no red dots/arrows. Each card clearly shows Pain and How we solve it with a check icon.

Feature cards have larger titles, a single optional blurb, and aligned plan badges at top-right. Icons are consistent size.

Text is readable on mobile. No clipping.

Hover is subtle. No gradients.

A11y: headings are semantic, cards readable by screen readers.

Run the update, refresh an industry page, and confirm both sections meet the criteria.