Short answer:
Your grid shows the same “Plug” icon because the component hard-codes a single icon. Fix it by driving the section from a **feature metadata map** that sets a unique icon and a **feature route slug** per card, add the missing **9th** card, and clean the badge/spacing.

Use this prompt in Replit AI Agent to implement it exactly:

---

## PROMPT: “Feature grid — 9 cards, unique icons, linked, clean badges”

**Scope**

* Edit only the industry page feature section.
* Files to add/update:

  * `client/src/data/features.ts`  ✅ new
  * `client/src/sections/industries/IndustryFeatureGrid.tsx`  ✅ update
* Do not touch other sections or routing except adding stub routes if a feature slug doesn’t exist.

### 1) Create feature metadata

Create `client/src/data/features.ts`:

```ts
// Unique icons + slugs per feature used across industries
import {
  CalendarClock,        // scheduling
  ClipboardList,        // work orders + checklists
  Signature,            // e-sign
  Bell,                 // reminders & messaging
  RefreshCw,            // recurring/warranty visits
  Route as RouteIcon,   // route optimization + GPS
  Boxes,                // parts/inventory
  BarChart3,            // analytics
  ListChecks            // custom checklists & fields
} from "lucide-react";

export type Plan = "Free"|"Starter"|"Essential"|"Premium"|"Platinum";

export type FeatureMeta = {
  key: string;                   // stable key
  label: string;                 // card title
  slug: string;                  // /features/:slug
  minPlan: Plan;
  blurb?: string;
  Icon: any;                     // lucide icon component
};

// Canonical 9 features for industry pages (3 x 3 on desktop)
export const FEATURE_META: FeatureMeta[] = [
  {
    key: "scheduling",
    label: "Drag-and-drop scheduling (calendar sync)",
    slug: "scheduling",
    minPlan: "Free",
    blurb: "Two-way sync with Google & Apple Calendar.",
    Icon: CalendarClock
  },
  {
    key: "work-orders",
    label: "Work orders with checklists & photos",
    slug: "work-orders",
    minPlan: "Free",
    blurb: "Standardize jobs with checklists and photo notes.",
    Icon: ClipboardList
  },
  {
    key: "estimates-esign",
    label: "Estimates & e-signatures",
    slug: "estimates",
    minPlan: "Free",
    blurb: "Send quotes and get approvals from any device.",
    Icon: Signature
  },
  {
    key: "reminders",
    label: "Automated reminders & messaging",
    slug: "messaging-reminders",
    minPlan: "Starter",
    blurb: "Cut no-shows with SMS/email reminders.",
    Icon: Bell
  },
  {
    key: "recurring",
    label: "Recurring warranty/maintenance visits",
    slug: "recurring",
    minPlan: "Essential",
    blurb: "Plan service agreements and repeating jobs.",
    Icon: RefreshCw
  },
  {
    key: "routing-gps",
    label: "Route optimization & GPS",
    slug: "routing-gps",
    minPlan: "Essential",
    blurb: "Reduce windshield time with optimized routes.",
    Icon: RouteIcon
  },
  {
    key: "inventory-costing",
    label: "Parts inventory & job costing",
    slug: "inventory-costing",
    minPlan: "Premium",
    blurb: "Track parts, costs, and profitability.",
    Icon: Boxes
  },
  {
    key: "analytics",
    label: "Advanced reporting & analytics",
    slug: "analytics",
    minPlan: "Premium",
    blurb: "Dashboards and custom reports.",
    Icon: BarChart3
  },
  {
    key: "custom-checklists",
    label: "Custom checklists & fields",
    slug: "custom-checklists",
    minPlan: "Premium",
    blurb: "Capture exactly the data your team needs.",
    Icon: ListChecks
  }
];
```

> If some `/features/:slug` routes don’t exist, generate simple stub pages so links don’t 404.

### 2) Update the section to use metadata, link icons/titles, add 9th card, and polish styling

Edit `client/src/sections/industries/IndustryFeatureGrid.tsx`:

* Import `FEATURE_META`, `FeatureMeta`, and lucide is already handled via the `Icon` in metadata.
* Render **nine** cards from `FEATURE_META`.
* Each card links to `/features/${slug}` on both the **icon** and the **title**.
* Uniform badge in the **top-right**, absolute, equal padding.
* Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` → always 3×3 on desktop. No spanning hacks.

Replace the component body with:

```tsx
import { FEATURE_META, FeatureMeta } from "@/data/features";
import clsx from "clsx";
import { Link } from "react-router-dom";

const PLAN_STYLES: Record<string, string> = {
  Free:      "bg-slate-100 text-slate-700",
  Starter:   "bg-blue-100 text-blue-700",
  Essential: "bg-emerald-100 text-emerald-700",
  Premium:   "bg-violet-100 text-violet-700",
  Platinum:  "bg-amber-100 text-amber-800",
};

export default function IndustryFeatureGrid() {
  const items: FeatureMeta[] = FEATURE_META; // always 9

  return (
    <section aria-label="Every feature you need">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14">
        <div className="text-center mb-6 sm:mb-8">
          <h2 className="text-xl sm:text-2xl font-semibold">Every feature you need</h2>
          <p className="text-slate-600 mt-1">Start free, upgrade as you grow</p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-5">
          {items.map(({ key, label, slug, minPlan, blurb, Icon }) => (
            <article
              key={key}
              className={clsx(
                "relative rounded-2xl border border-slate-200 bg-white shadow-sm",
                "transition duration-150 ease-out hover:shadow-md hover:-translate-y-0.5",
                "motion-reduce:transition-none motion-reduce:hover:transform-none motion-reduce:hover:shadow-sm"
              )}
            >
              {/* Plan badge */}
              <span
                className={clsx(
                  "absolute top-3 right-3 inline-flex items-center justify-center",
                  "px-2 py-0.5 rounded-full text-xs font-medium",
                  PLAN_STYLES[minPlan]
                )}
              >
                {minPlan}
              </span>

              <div className="p-4 sm:p-5 h-full">
                {/* Icon links to feature page */}
                <Link
                  to={`/features/${slug}`}
                  aria-label={`Open ${label}`}
                  className="inline-flex items-center text-slate-400 hover:text-[#0B5FFF] focus:outline-none focus-visible:ring-2 focus-visible:ring-[#0B5FFF] focus-visible:ring-offset-2 rounded-md"
                >
                  <Icon className="w-6 h-6 sm:w-7 sm:h-7" aria-hidden="true" />
                </Link>

                {/* Title links to feature page */}
                <h3 className="mt-3 text-[15px] sm:text-base font-medium text-slate-900 leading-snug line-clamp-1">
                  <Link
                    to={`/features/${slug}`}
                    className="hover:underline underline-offset-2"
                    aria-label={`Learn more about ${label}`}
                  >
                    {label}
                  </Link>
                </h3>

                {blurb ? (
                  <p className="mt-1 text-sm text-slate-600 line-clamp-2">{blurb}</p>
                ) : null}
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  );
}
```

### 3) Routing for feature pages

* Ensure routes like `/features/scheduling`, `/features/estimates`, etc. exist.
* If missing, create stubs in your features route folder with a standard feature template (title, 1-2 paragraphs). This is just to avoid dead links now.

### 4) Acceptance criteria

* Exactly **9** cards render (3×3 on desktop, 2×? on tablet, 1×? on mobile).
* Each card shows the **correct unique icon** tied to its feature.
* **Icon and title are links** to `/features/:slug`.
* Plan badge is top-right with symmetrical padding. No random extra space.
* Fonts readable, no clipping. Hover is subtle. Keyboard focus ring visible on links.
* No TypeScript errors and no 404s for linked features.

---

This gives you a clean, on-brand grid, distinct icons that match the feature, the missing 9th card, and real links to feature pages.
